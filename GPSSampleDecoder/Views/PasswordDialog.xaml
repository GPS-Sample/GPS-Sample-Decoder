<Window x:Class="GPSSampleDecoder.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GPSSampleDecoder.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Enter a Passcode" Height="180" Width="350">
    <Grid Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin ="20,20,0,0" Text="Enter the password used to encrypt this JSON file." VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin ="20,2,0,0" Text="Leave blank if default password was used." VerticalAlignment="Center"/>
        <PasswordBox  x:Name="PasswordTextBox"   Grid.Column="0" Grid.Row="2" Margin="20,10,20,0" Grid.ColumnSpan="2"  />
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" Margin="0,20,20,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="okButton" Click="okButton_Click" Width="30" Height="20" IsDefault="True" Margin="0,0,10,0">Ok</Button>
            <Button Name="cancelButton" Click="cancelButton_Click" IsCancel="True" Margin="0,0,0,0" Width="60" Height="20">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
